pipeline {
  agent any
  stages{
      stage('Build'){
        steps {
          echo 'Running Build Automation'
          sh './gradlew --no-daemon'
          archiveArtifacts artifact: 'dist/trainSchedule.zip'
        }
      }
  }
}
